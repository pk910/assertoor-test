
network_params:
  deneb_fork_epoch: 2
  genesis_delay: 120
  seconds_per_slot: 12
  num_validator_keys_per_node: 250
launch_additional_services: true
additional_services:
  - assertoor
snooper_enabled: false
assertoor_params:
  tests:
  - "$TESTS/stability-check.yaml"
  - "$TESTS/block-proposal-check.yaml"
  - "$TESTS/eoa-transactions-test.yaml"
  - "$TESTS/blob-transactions-test.yaml"
  - "$TESTS/all-opcodes-transaction-test.yaml"

